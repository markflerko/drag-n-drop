export type CoordsType = {
  x: number,
  y: number
}

export type ContainerType = {
  top: number,
}

export type FigureDataType = {
  x: number,
  y: number,
  name: string,
  width: number,
  height: number,
  id: number,
}